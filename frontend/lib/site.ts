/**
 * External links surfaced in the header and footer.
 *
 * TODO(lideeyah): replace both with the real URLs before submission. They are
 * centralised here rather than inlined so there is exactly one place to edit,
 * and so a placeholder can never hide in the middle of a component.
 */
export const GITHUB_URL = "https://github.com/lideeyah/magno";
export const SUBMISSION_URL = "https://lablab.ai/";
